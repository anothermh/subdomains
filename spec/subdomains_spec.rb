require 'spec_helper'

describe Subdomains do
  it 'has a version number' do
    expect(Subdomains::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
